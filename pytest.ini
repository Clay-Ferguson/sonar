# Test settings. Kept out of pyproject.toml so that file stays what it is —
# the app's two runtime dependencies and nothing else.
[pytest]
testpaths = tests
# tests/ is on the path so `from helpers import ...` works without making the
# directory a package; the repo root is added by conftest so `sonarex` imports.
pythonpath = tests
